/**
 * Compatibility entrypoint for hosts that still start the push server from the
 * repository root. The canonical implementation lives in push-server/server.js.
 */
import "./push-server/server.js";
